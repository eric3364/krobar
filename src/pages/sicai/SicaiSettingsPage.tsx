import { SicaiPlaceholder } from "./SicaiPlaceholder";

export default function SicaiSettingsPage() {
  return (
    <SicaiPlaceholder
      title="Paramètres SICAI"
      description="Configuration de la fonction SICAI (modèles, seuils, dictionnaires)."
    />
  );
}
